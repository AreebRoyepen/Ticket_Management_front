import React from "react";
import "../../../styles/eventCard.css";

export default function NoCard()
{
    return( <div>
      <div class="noCard">There are currently no events to view.</div>
    </div>);
}

           